{
  "name": "@pocket-agent/hostd",
  "version": "1.14.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pocket-agent/codex-bridge": "workspace:*",
    "@pocket-agent/remote-protocol": "workspace:*",
    "@pocket-agent/security": "workspace:*",
    "@pocket-agent/session-store": "workspace:*",
    "@pocket-agent/workspace-manager": "workspace:*",
    "ws": "^8.18.3",
    "zod": "^3.24.2"
  }
}
